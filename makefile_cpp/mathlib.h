#ifndef MATHLIB
#define MATHLIB

// namespaces are used to access these functions like
// for example mathlib::add(a, b);
namespace mathlib
    {
        float add(float*, float*);
        float subtract(float*, float*);
        float multiply(float*, float*);
    }

#endif